var http = require('http');

var server = http.createServer(function (require, response)
{
	//handle incoming request here..
});

server.listen(5000);

console.log('Node.js webserver at port 500 is running...')